<Window x:Class="AionPingLite.ChartWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="By 羚羊 qq:976950177" Height="300" Width="450"
        WindowStyle="ToolWindow" Topmost="True" Background="#1E1E1E">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock x:Name="StatsText" Foreground="White" FontSize="14" FontWeight="Bold" Margin="0,0,0,10"/>
        
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="20"/>
            </Grid.RowDefinitions>

            <!-- Y Axis labels -->
            <Canvas x:Name="YAxisCanvas" Grid.Column="0" Grid.Row="0"/>

            <!-- Chart area -->
            <Border Grid.Column="1" Grid.Row="0" BorderBrush="#555" BorderThickness="1,0,0,1">
                <Canvas x:Name="ChartCanvas" Background="#2D2D30"/>
            </Border>

            <!-- X Axis labels -->
            <Canvas x:Name="XAxisCanvas" Grid.Column="1" Grid.Row="1"/>
        </Grid>
    </Grid>
</Window>
